<div class="container">
  <h1>Welcome to <strong>somename</strong></h1>
  <p>Find anything about contruction!</p>

  <div>
    <% @posts.each do |post| %>
    <%= link_to post_path(post), class: "text-decoration-none" do %>
    <div>
      <h2><%= post.title%></h2>
      <p><%= post.content%></p>
    </div>
      <% end %>
    <% end %>
  </div>
</div>
